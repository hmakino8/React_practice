import { iconClock } from "../style/styleTaskList";

export const Clock = () => {
  return (
    <span className="material-symbols-outlined" style={iconClock}>
      schedule
    </span>
  );
};

export const MoreVert = () => {
  return <span className="material-symbols-outlined">more_vert</span>;
};

export const ToggleMenuProject = () => {
  return <span className="material-symbols-outlined">menu</span>;
};
